name: First Try

on: push

jobs:
    test-install:
        runs-on: ubuntu-latest

        steps: 
            - name: Greeting
              run: echo "Hello world"
        
            - name: Check Python
              run: python --version
            
            - name: Check Docker
              run: docker --version

    